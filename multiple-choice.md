# Multiple Choice Questions

1. When we use `git pull` should we use it before or after we merge?
    a. We use `git pull` **before** we merge
    a. We use `git pull` **after** we merge
    a. We do not use `git pull` **at all** 
    a. We use `git pull` **when we start a new project** 

2. When we collaborate with other developers, it is important that we 
    a. We use branches and we follow git collaboration protocols
    b. Only work on main
    c. Only have one branch for everything 
    d. Use the fast forward method

3. 

4. 

5. 

6. 

7. 

8. 

9. 

10. 
